import Link from 'next/link';
import { getCategories, getProjects, getSite } from '@/lib/content';
import { ProjectCard } from '@/components/project-card';
export default function Home() { const site = getSite(); const categories = getCategories(); const featured = getProjects().slice(0, 4); return <><section className="section"><div className="container grid items-center gap-12 lg:grid-cols-[1.15fr_.85fr]"><div><p className="eyebrow mb-5">سلام، من {String(site.name)} هستم</p><h1 className="display max-w-4xl">{String(site.title)}<span className="text-[var(--primary)]">.</span></h1><p className="mt-7 max-w-2xl text-lg text-[var(--muted)]">{String(site.bio)}</p><div className="mt-9 flex flex-wrap gap-3"><Link className="button" href="/projects">دیدن پروژه‌ها <span aria-hidden>←</span></Link><Link className="button secondary" href="/about">بیشتر درباره من</Link></div></div><div className="relative mx-auto"><div className="absolute -inset-5 rounded-full border border-[var(--primary)] opacity-20"/><img src={String(site.profileImage)} alt={`تصویر ${String(site.name)}`} className="relative h-64 w-64 rounded-full border-4 border-[var(--primary)] object-cover sm:h-80 sm:w-80"/></div></div></section><section className="section border-t border-[var(--border)]"><div className="container"><div className="mb-9 flex items-end justify-between"><div><p className="eyebrow">منتخب</p><h2 className="mt-2 text-3xl font-bold">پروژه‌های اخیر</h2></div><Link className="text-sm text-[var(--primary)]" href="/projects">مشاهده همه ←</Link></div><div className="grid gap-5 md:grid-cols-2">{featured.map((project) => <ProjectCard key={project.slug} project={project} categories={categories}/>)}</div></div></section></>; }
