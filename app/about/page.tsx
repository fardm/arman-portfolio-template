import { getSite } from '@/lib/content';
export default function AboutPage() { const site = getSite(); return <section className="section"><div className="container grid max-w-5xl gap-12 md:grid-cols-[.7fr_1.3fr] md:items-center"><img src={String(site.profileImage)} alt={`تصویر ${String(site.name)}`} className="mx-auto h-64 w-64 rounded-full border-4 border-[var(--primary)]"/><div><p className="eyebrow">کمی درباره من</p><h1 className="mt-2 text-5xl font-black">طراحی با فکر، ساخت با دقت.</h1><p className="mt-7 text-lg leading-loose text-[var(--muted)]">من {String(site.name)} هستم؛ {String(site.title)}. از تبدیل مسئله‌های پیچیده به تجربه‌هایی ساده و قابل فهم لذت می‌برم. در پروژه‌ها به جزئیات، گفت‌وگو با تیم و اثر واقعی محصول اهمیت می‌دهم.</p></div></div></section>; }
